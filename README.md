amoeba-deploy-tools
===================

gem install amoeba-deploy-tools

Example .amoeba/config
----------------------

    [node "www.example.com"]
        role = app-rails
    [alias]
        sudo = exec sudo
